const multiplicarPorDois = (x) => {
  return x * 2;
};

console.log(multiplicarPorDois(2));

/* simplificado a operaçãoa acima,cuidado so deve se usado para simplificar se atrapalar a leitura do codigo não usar */
let multiplicarPorDoiss = (xx) => xx * 2;
console.log(multiplicarPorDoiss(20));
