let numeros = [0, 1, 2, 3, 4, 5];
console.log(numeros.slice(3, 4)); // pegaa ate o ndice menos 1

console.log(numeros.slice(0)); // pega todos se n colocar o segundo numero

//numeros negativos e do ultimo para o primeiro
console.log(numeros.slice(-3));

console.log(numeros.slice(-1));
